import { Inter, Bebas_Neue, Lusitana } from "next/font/google";

export const inter = Inter({ subsets: [ 'latin' ] });

export const bebas = Bebas_Neue({ subsets: [ 'latin' ], weight: "400" });

export const lusit = Lusitana({subsets: ['latin'], weight: "400"});